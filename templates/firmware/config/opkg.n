OPKG_RMPKG_ROOT -> OPKG_ROOT
OPKG_RMPKG_ARCH -> ARCH
OPKG_INSTALL_ROOT -> OPKG_ROOT
OPKG_INSTALL_ARCH -> ARCH
OPKG_ROOT := ${FBAU_ROOTFS_ROOT}
ARCH := ${FBAU_DEFAULT_ARCH}
OPKG_INSTALL_PACKAGE_DIR := ${FBAU_IPK_INSTALL_DIR}
__NEXT_CONFIG := ${FBAU_CURRENT_NODE_PATH}/${FBAU_NODE_SUFFIX}